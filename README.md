# resume

## DogWalk Social prototype
A mobile MVP starter is available in `dogwalk-mobile/`.
It includes a Phase 2 Supabase integration path (auth + persistent walks/warnings + follows).

## Deploy as a standalone repository (Dog-app-one)
Use `DOG_APP_ONE_DEPLOYMENT.md` and `scripts/bootstrap-dog-app-one.sh` to split and publish the mobile app as its own repository.
